language: c

compiler:
  - gcc
  - clang

install:
 - sudo apt-get update
 - sudo apt-get install libssl-dev
 - sudo apt-get install libevent-dev

script:
  - git submodule update --init --recursive
  - make